import { AssetLibraryLocationMetadata } from "@uniformdev/mesh-sdk-react";

// Settings for the integration, used for Umami analytics dashboard
export type IntegrationSettings = {
  srcUrl: string;
  initialHeight?: number;
};
